

alias measurement get_tran {
	run tran
	export real SR = ((max(V(VOUT)) - min(V(VOUT)))*0.80)/falltime ( sig=V(VOUT), initval=max(V(VOUT)), inittype='y, finalval=min(V(VOUT)), finaltype='y, theta1=90, theta2=10)
}


foreach data {
	run get_tran
}
